{
  "name": "planets",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backoffice": "pnpm --filter backoffice dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "generate:api": "pnpm --filter shared-types generate"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
