// Ce fichier est généré automatiquement par `pnpm generate:api`.
// Lancé la première fois après `docker compose up` ou `uv run uvicorn ...`

export interface paths {}
export interface components {}
export interface operations {}
